/**
 * Index file for controllers.
 *
 * @fileoverview Index file for controllers.
 */

const { TodoController } = require("./todo.controller");

/**
 * Entry point for controllers.
 *
 * @module controllers
 */
module.exports = {
  /**
   * TodoController class for handling HTTP requests related to the todo resource.
   * @type {TodoController}
   */
  TodoController,
};
